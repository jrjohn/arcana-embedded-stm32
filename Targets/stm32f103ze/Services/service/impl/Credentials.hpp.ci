#pragma once

// CI placeholder credentials — not used in production
#define WIFI_SSID_VALUE      "ci-ssid"
#define WIFI_PASS_VALUE      "ci-password"
#define MQTT_BROKER_VALUE    "localhost"
#define MQTT_PORT_VALUE      1883
