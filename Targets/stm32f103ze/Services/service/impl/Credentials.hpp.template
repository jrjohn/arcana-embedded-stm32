#pragma once

// Local credentials — copy this file to Credentials.hpp and fill in real values
// Credentials.hpp is gitignored and will NOT be committed

#define WIFI_SSID_VALUE      "YOUR_SSID"
#define WIFI_PASS_VALUE      "YOUR_PASS"
#define MQTT_BROKER_VALUE    "YOUR_BROKER"
#define MQTT_PORT_VALUE      1883
